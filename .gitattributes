# Release archives are external artifacts and are ignored by .gitignore.
# Do not add ZIP payloads to the source repository or Git LFS; publish them
# through GitHub Releases/Zenodo and record checksums under release/.
