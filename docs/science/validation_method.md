# Validation method

Validation is split into development, validation and deterministic blind-test
sets. Known-target recovery, injection-recovery completeness, negative controls,
parameter recovery, interval coverage, FPP calibration and independent baseline
comparison are separate measurements.

Each report includes schema version, input/config/output hashes, git revision,
random seed and limitations. A measured value is never inferred from an absent
corpus.